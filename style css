/* ============================================   Eco Trend Boutique — style.css   External stylesheet linked by index.html   ============================================ *//* ---- 2.2 Base style / mini CSS reset ---- */*, *::before, *::after{ box-sizing:border-box; }html, body, h1, h2, h3, p, ul{ margin:0; padding:0; }img{ max-width:100%; display:block; }:root{  --moss:#2f3b28;  --sage:#7c8f6a;  --linen:#f2ede1;  --clay:#b5652d;  --bark:#5a4636;  --line:izina(47,59,40,0; }  html{ scroll-behavior:smooth; }  body{r-box; }  html{ scbackground:var(--linen);    colserifr(--moss);    font-family:'Work Sans', sans-serif;    font-size:16px;   'Fraunces', serif;    font-weight:600font-family:'Fraunces', serif;    font-weight:600;    margin:0 0 .5em; width:62ch; }  a{ color:inherit; }  ul{ padding-left::62ch; }  a{ color:inherit; }  ul{ padding-left:1.1em; ----img{ max-wid/ Nav0%;------ay:block; }  /* ---------- Headey;/ Nav ---------- */  header{    position:sticky; top:0; z------- */  header{    position:sticky; top:0; z-iotto:50;    background:var(--li  .n  w border  max-width:var(- var(--line);  }  .nav-wrap{a   max-width:var(--max)ter; justify-content:space-y:flex; align-items:center; justify-content:space-between;    padding:16px 24px;  }  .brand{    font-family:'Fraunces', serif;    font-size:1.3rem;    font-weight:600n-items:center; gap:.5emem;    display:flex; align-items:center; gap:.5em;  }  .brand-mark{    width:26px; height:26px; border-radius:50%;    background:var(--sage);    display:inline-block;  }  nav ul{    list-style:none; display:flex; gap:28px; margin:0; padding:0;  }  nav button{    background:none; border:none; cursor:pointer;    font-family:'Work Sans', sans-serif;    font-size:.95rem;    color:var(--moss);    padding:6px 2px;    border-bottom:2px solid transparent;    transition:border-color .2s ease, coocus-visible{ color:var(--tton:hover, nav button:focus-visible{ color:var(--clay); }--clay);utton.active{    border-bottom-color:var(--clay);    color:var(--clay);    font-weight:500;  }  .menu-toggle{    display:none;    background:none; border:none; cursor:pointer;    flex-direction:column; gap:5px; padding:6px;  }  .menu-toggle span{    wpdth:24px; height:2px; background:var(--moss); display:block;  }  /* ---------- ); margie:0 auto; padding:0 24px; }  section{ display:none; padk; animation:fade .5s ease; }  @keyframes fade{:.3or:vanter; o  mns:1.g-top9frpx;  48px; align-items:center;    padding-top:40px;  }  .hero h1{ font-size:clamp(2.2rem, 4vw, 3.2;  }  .hero h1{ font-size:clamp(2.2rem, 4vw, 3.2;  }  .hero h1{ font-size:clamp(2.2rem, 4vw, 3.2;  }  .hero h1{ font-size:clamp(2.2rem, 4vw, 3.2rem); }  .hero-vifoal{    aspect-ratio:4/5; border-radius .hero-visual{    aspect   radi4/5; border-radius:2px;    background:      radi4/5; border-radius:2px;    background:      radi4/5; border-radius:2px;    background:      radial-gradient(circle at 30% 20%, rgba(181,101,45,.35), transparent 55%),      linear-gradient(160deg, var(--sage) 0%, var(--moss) 100%);    position:relative; overflow:hidden;  }  .hero-visual::after{    content:"";    position:absolute5,.4);  }  .cta-row{ display:flex; ga(242,237,225,.4);  }  .cta-row{ display:flex; gap:   displ.95rem; text-decoration:none; cursor:pointer;    border:1px solid ration:none; cursor:pnd:none; color:var(--mos(--linenb; }  .btn.primary:hover, .btn.primary:focus-visible{ backgroprimary:hover, .btn.primary:focus-visible{ backgrounimary:hover, .btn.primary:focus-visible{ background:vay:hover, .btn.primary:focus-visible{ background:vay:hoveay); border-color:var(--clay); }  .btn:not(.primary):hover, .btn:n:var(--clay); }  .btn:not(.primary):hover, .btn:n:var(--clay); }  .btn:not(.primary):hover, .btn:not(.primary):focus-visible{ background:var(--moss); color:var(-ocus{ oupx; .benefits{    margin-top:72px; padding-top:48px; .benefits{    margin-top:72px; padding-top:48px; bbenefits{    margin-top:72px; padding-top:48px; border-top:1px solid var(--line);    display:grid; grid-template-columns:ree:1.05,1k; Fraunces',serif; font-size:1.4rem; display:block; Fraunces',serif; font-size:1.4rem; display:block; Fraunces',serif; font-size:1.4rem; display:block; margin-bottom:.2em;}y:flex; gap:16pxy); margin-top:9p-radius:50%; background:var(--clay); margin-top:9px; dius:50%; background:var(--clay); margin-top:9px; dius:50% seri; } }  .audience-grid{ disp:20px; margin-t--line); padding margin-bottom:.n:0; }  /* Products */p{ font-size:.92rem; margin:0; }  /* Products g{ color:var(--padding:32px; }  .product-card .tag{ color:var(--padding:32px; }  .product-card .tag{ color:var(--padding:32px; }  .product-card .tag{ color:var(--padding:32px; }  .product-card .tag{ color:var(--padding:32px; }  .product-card .tag{ color:var(--padding:32px; }  .product-card .tag{ color:var(--padding:32px; }  .product-card .tag{ color:var(--padding:32px; }  .product-card .tag{ color:var(--padding:32px; }  .product-card .tag{ color:var(--padding:32px; }  .product-card .tag{ color:var(--padding:32px; }  .product-cmol 2i{gl14pxeu;rplr  t}{ })})})})})})})})})})})}})}}))}})})})}}))}}})}